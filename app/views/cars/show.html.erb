<%= @car.car_name %>
<br>
<% if @car.photos.attached? %>
	<% @car.photos.each do |photo| %>
		<%= image_tag photo, width: 400, height: 300%>
	<%end%>
<%else%>
	<p>No photo found.</p>
<%end%>

<p>Category: <%= @car.category %></p>
<p>Capacity: <%= @car.capacity %></p>
<p>Price: RM <%= @car.price %> per day</p>

<%=link_to 'Home', root_path%>
